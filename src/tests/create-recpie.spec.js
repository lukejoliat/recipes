/* eslint-disable no-undef */
import CreateRecipe from '../components/create-recipe/create-recipe'

it('should toggle the checked property', async () => {
  const createRecipe = new CreateRecipe()

  expect(createRecipe).toBeTruthy()
})
